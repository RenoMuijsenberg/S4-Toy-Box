import SearchBar from "../components/SearchBar";
import Navbar from "../components/navbar";

const Home = () => {
    return (
        <div>
            <SearchBar/>
            <Navbar/>
        </div>
    );
};

export default Home;